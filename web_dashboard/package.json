{
  "name": "modbus-web-dashboard",
  "version": "1.0.0",
  "description": "Local web dashboard for Modbus TCP simulator — polls all devices every 1s, displays on web UI, allows demand_control_power write via FC06.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "modbus-serial": "^8.0.16"
  }
}
